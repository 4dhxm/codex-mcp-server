# Server Configuration
PORT=3000

# Authentication
# Generate a secure key: node -e "console.log(require('crypto').randomUUID().replace(/-/g,''))"
CODEX_MCP_API_KEY=

# OpenAI (required by @openai/codex-sdk)
OPENAI_API_KEY=
